# 2026-09-15

## 代码更新（同步 dev → main）
- 用户在 D:\Dev\Cyper-Idle 请求"更新下代码"。
- 起点：本地 main 停在 efa9f6d（7月），本地 dev / origin/dev 已到 9b1f722（8月21日：glitch 视觉、任务完成追踪、侧栏交互、通知系统）。
- 处理：main 快进合并 origin/dev（9b1f722）；随后发现 GitHub main 上还有 PR #8 合并提交 82d9064（与 9b1f722 树完全一致），又快进到 82d9064。
- 结果：本地 main = GitHub main = 82d9064，工作区干净。涉及 7 个文件、+671/-140（css/style.css、data/ui.json、index.html、python/engine/quest.py、state.py、story.py、python/main.py）。
- 附带处理：dev 分支上有一个环境自动生成的 checkpoint 提交 9e19b2a（README.zh-CN.md 微调），未合入 main（非用户改动）。
- 注意：本环境 SSH remote（git@github.com）拉取被拒（publickey），需用 HTTPS URL 直接 fetch https://github.com/CY202227/Cyper-Idle.git。
- 注意：环境会自动还原 origin/* 跟踪引用（update-ref 后被重置回旧值），导致 status 永远显示 "ahead"，属显示问题，不影响实际同步状态。以 rev-parse main 与 GitHub 实际 HEAD 比对为准。
- 语法检查：py_compile main.py + engine/quest.py + state.py + story.py 全部通过。
